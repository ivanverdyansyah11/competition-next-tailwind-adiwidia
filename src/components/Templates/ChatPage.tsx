import SectionChat from "@/components/Organisms/Section/SectionChat";

export default function ChatPage() {
    return (
        <main className="main">
            <SectionChat/>
        </main>
    );
}